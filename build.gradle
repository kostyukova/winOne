plugins {
    id 'idea'
    id 'eclipse'
}

version '1.0_alpha'

subprojects {
	apply plugin: 'java'
	tasks.withType(JavaCompile) {
		sourceCompatibility = 1.8
		targetCompatibility = 1.8
	}
	repositories {
		jcenter()
	}
}